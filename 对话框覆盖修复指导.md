# 对话框覆盖修复指导

## 1. 修复目标

当前中文模式的问题不是单纯的翻译质量问题，而是文字框、气泡轮廓和中文覆盖层之间的边界不够可靠：

- OCR 返回的是文字矩形，不是完整气泡。
- 浅色背景可能被误判为气泡内部。
- 同一气泡的多行文字可能被分别覆盖，造成重叠。
- 不规则气泡的外接矩形会超出实际边界。
- 没有气泡背景的拟声词、语气词被强行覆盖，反而影响画面。

本轮修复应优先保证覆盖范围正确。无法确定对话框边界时，保留原图比绘制一个过大的中文框更好。

## 2. 当前代码链路

### 2.1 OCR

入口在 `backend/worker.py` 的 `recognize()`：

```text
MangaPageOcr -> blocks[]
每个 block 包含：id、box、source、vertical、translation
```

其中 `box` 只是文字区域 `[left, top, right, bottom]`。OCR 不会告诉界面：

- 文字属于哪个气泡；
- 气泡的完整轮廓；
- 气泡是否有尾巴；
- 文字是否位于无背景的拟声词或画面文字中。

### 2.2 气泡推测

入口在 `windows_app.py` 的 `detect_speech_region()`：

1. 以 OCR 框为中心扩大裁剪区域。
2. 转灰度图并按亮度阈值提取浅色区域。
3. 使用形态学操作连接被日文字符切开的区域。
4. 查找包含 OCR 框中心的亮色连通区域。
5. 将最大区域转换为多边形。
6. 用多边形外接矩形推算中文安全区。

这个方法本质上是启发式猜测，不是真正的气泡检测。浅色分镜背景、断开的边框和气泡之间的空隙都可能让候选区域被错误合并。

### 2.3 中文覆盖

入口在 `ImageCanvas.paintEvent()` 的中文模式：

- 找到候选轮廓时绘制半透明多边形；
- 根据 `text_box` 绘制中文；
- 找不到轮廓时扩大 OCR 矩形并绘制圆角矩形；
- 当前不会真正擦除原文，半透明底色下仍可能看到日文或背景。

## 3. 本轮推荐处理策略

### 阶段 A：建立覆盖安全阀

先给每个 block 增加覆盖状态，建议使用以下字段：

```json
{
  "overlay": "balloon | rectangle | skip | uncertain",
  "overlay_score": 0.0,
  "overlay_reason": ""
}
```

规则如下：

- `balloon`：轮廓闭合、面积合理、边界对比明显，可以覆盖。
- `rectangle`：无法得到气泡轮廓，但 OCR 框附近确实像一个规则文字框，可以使用保守矩形。
- `skip`：无可靠背景，或属于低影响短语气词/拟声词，不翻译、不覆盖。
- `uncertain`：检测结果不可靠，只显示 OCR 框，不生成大面积中文层。

硬性限制：

- 候选区域不能接近裁剪区域的全部面积；建议先拒绝超过裁剪区 `50%~60%` 的候选。
- 候选区域相对 OCR 框过大时拒绝；初始建议限制在文字框面积的 `4~8` 倍内，并按图片分辨率调参。
- 候选轮廓必须有足够的边缘对比和闭合程度。
- 候选触碰裁剪边缘时降低置信度；只有在有明确边界证据时才允许通过。
- 多个 block 生成的候选区域重叠超过 `50%` 时，只保留得分最高的一个。

低置信度时的行为必须是“不覆盖”，而不是扩大矩形继续猜测。

### 阶段 B：合并同一气泡的多行文字

在调用 `detect_speech_region()` 前先对 OCR block 做分组。两个文字框满足以下条件时可以视为同一气泡候选：

- 中心点距离较近；
- 文字方向一致；
- 框之间没有明显的粗黑分镜线；
- 合并后的区域长宽比和面积仍然合理。

每组只检测一次气泡轮廓，再把同组译文合并绘制。这样可以避免一行日文生成一个覆盖层，也能给中文留出完整排版空间。

### 阶段 C：加入轮廓质量评分

对每个候选计算评分，建议至少包含：

```text
score =
  0.30 * OCR 框覆盖率
  + 0.25 * 边界闭合度
  + 0.20 * 边界明暗对比
  + 0.15 * 面积合理度
  + 0.10 * 轮廓实心度
```

建议阈值：

- `score >= 0.72`：允许使用气泡轮廓；
- `0.48 <= score < 0.72`：只允许小范围矩形覆盖；
- `< 0.48`：标记 `uncertain`，不覆盖。

阈值应通过真实漫画样本调整，不要为了提高覆盖率而降低到会误选整块分镜背景的程度。

### 阶段 D：短语气词和无背景文字跳过

用户明确允许暂时不翻译没有对话框背景的语气词。建议将它定义为独立的预过滤规则，而不是让轮廓检测承担这个判断。

建议新增：

```python
def is_low_impact_utterance(source: str, box: list[float], region) -> bool:
    ...
```

初始判定可以使用以下条件组合：

1. `region is None` 或轮廓分数低于安全阈值；
2. 去除标点、空格后长度不超过 4 个字符；
3. 内容主要由假名、重复音、感叹号或省略号组成；
4. 不包含明显的名词、动作、人物称呼或完整句子。

可优先列入低影响候选的例子：

```text
ん / え / あ / お / へえ / うん / まあ / あっ / えっ
ふっ / はあ / んん / …… / ！？ / ふふ
```

注意：这个列表只能作为保守起点。`そう`, `はい`, `いや`, `なに` 等词有时承载实际语义，不应仅凭长度直接跳过。遇到不确定内容，应保留 OCR 框或右侧原文，不生成错误中文覆盖。

推荐行为：

- OCR 仍然保留该 block，方便检查；
- 翻译任务中不把 `skip` block 放入 `target_ids`；
- 右侧显示“原图保留”或“暂不覆盖”，而不是显示错误的“暂无有效中文译文”；
- 中文模式不绘制覆盖层；
- 文字框模式仍可显示该 block 的位置。

这样既不会影响阅读，也不会为了翻译一个小语气词覆盖人物或背景。

### 阶段 E：计算真实文字安全区

当前 `text_box` 是轮廓的轴对齐外接矩形，容易让文字越过不规则边界。后续应改为：

1. 对候选轮廓生成内部 mask；
2. 使用距离变换计算边界到内部的距离；
3. 以最大内接区域作为中文排版区域；
4. 文字绘制前再与 mask 求交，避免超出气泡；
5. 对狭长气泡根据 `vertical` 和可用宽高决定横排或竖排。

在这一步完成前，不要把不规则轮廓的文字区域强行扩大到整块外接矩形。

### 阶段 F：覆盖层透明度和擦字

轮廓识别稳定后，再处理原文残影：

- 纯白或浅色气泡使用接近不透明的填充；
- 带纹理气泡使用局部颜色估计或修复算法；
- 不要用大面积半透明青色覆盖人物、头发和分镜线；
- 擦字失败时保留原图并降低覆盖等级，不要扩大修复范围。

## 4. 建议代码落点

### `backend/worker.py`

- 在 `recognize()` 后增加 block 分类字段；
- 在 `translate()` 生成 `targets` 前排除 `overlay == "skip"` 的 block；
- 保存跳过原因，便于右侧面板解释状态。

### `windows_app.py`

- 保留 `detect_speech_region()` 作为候选生成器；
- 新增候选评分、面积限制和重叠去重；
- 在 `ImageCanvas` 中缓存 `overlay` 结果；
- `skip` 和 `uncertain` 不绘制中文覆盖；
- 气泡候选只绘制自己的 mask，不使用整块外接矩形填充；
- 文字框模式可以用不同颜色区分 `balloon`、`rectangle`、`skip` 和 `uncertain`。

### 缓存兼容

缓存版本应从 `version: 1` 升级到 `version: 2`。读取旧缓存时：

- 缺少新字段就重新计算覆盖状态；
- 不删除原文和已有手动译文；
- 手动修改的译文不能被 `skip` 规则覆盖或清空。

## 5. 验收标准

使用至少四类样本测试：标准方框、椭圆气泡、带尾巴气泡、不规则爆炸框，以及无背景语气词。

每类样本都检查：

- 覆盖层不跨越明显的分镜边界；
- 同一气泡不会出现多个重叠覆盖层；
- 中文文字不越过气泡轮廓；
- 检测失败时不会出现整块分镜大小的覆盖层；
- 无背景的“嗯、啊、诶”等短语气词保留原图，不进入翻译任务；
- 右侧仍能看到原文和跳过原因；
- 原图模式、文字框模式和中文模式切换正常；
- 滚轮缩放、拖动平移不会重复计算或改变覆盖区域；
- 旧缓存、手动译文和模型切换行为不回归。

建议增加自动化测试：

- 合成白底椭圆气泡：应返回 `balloon`；
- 合成浅色大分镜背景：应返回 `uncertain`，不能返回整块背景；
- 两个相邻气泡：应得到两个互不大面积重叠的候选；
- 无背景的 `ん`, `えっ`, `ふふ`：应返回 `skip`；
- 完整句子即使很短，也不能仅凭长度直接跳过。

## 6. 推荐实施顺序

1. 加入 `overlay` 状态和低置信度不覆盖的安全阀。
2. 限制候选面积、边缘和轮廓质量，修复整块分镜被覆盖的问题。
3. 合并同一气泡的多个 OCR block，并做重叠去重。
4. 加入短语气词跳过翻译规则。
5. 用距离变换替换轴对齐 `text_box`。
6. 最后再加入擦字和局部背景修复。

每完成一个阶段都应使用真实漫画页面回归测试，确认覆盖范围没有扩大，再继续下一阶段。
