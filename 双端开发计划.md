# Koma 双端开发计划（首版）

## 首版范围

首版聚焦漫画或图片文件夹：打开直接包含图片的文件夹，识别日文对白，调用本机 Ollama 翻译为简体中文，显示文字框和中文覆盖，支持逐句编辑、保存、重译、翻译整文件夹和缓存复用。首版不纳入截图、全局快捷键、悬浮翻译、ZIP/PDF、子文件夹递归或专业擦字排版。

## 前端方案

推荐使用 **PySide6（Qt）** 实现 Windows 与 macOS 共用的前端。现有模型和业务全部在 Python：前端只需用 Qt 的图片画布、侧栏、文本编辑控件和 `QProcess` 启动 worker，能复用当前后端，不再引入 Node、Rust 或 Chromium。发布时仍将 Python、PyTorch、Ollama 和模型作为主要体积来源，桌面外壳大小不是主要约束。

方案比较：

- **Tauri + React**：适合已有 React 团队和长期网页式 UI，但要维护 Rust 宿主、React 状态、Python sidecar 及两端桥接；当前迁移成本高于 PySide6。
- **Electron + React**：工具链成熟，开发体验好，但会额外携带 Chromium；本项目已有 Torch/Ollama 的内存和体积开销，首版收益不足。
- **WinUI/WPF**：Windows 原生体验较好，但会让 Windows 和现有 SwiftUI macOS 维护两套界面与控制器；只有明确接受双 UI 长期同步时再选。

## 共享后端接口

继续以 `backend/worker.py` 为唯一 OCR/翻译进程。前端为每张图片启动独立进程，传入：

```text
--image <图片路径>
--operation ocr|translate|both
--model <本地 Ollama 模型名>
--status <临时状态 JSON 路径>
--block-id <可选，单句重译>
```

stdout 保持单个机器可读 JSON：`{"ok": true, "cache": "..."}`，失败时为 `ok:false,error,cache`。处理中进度写入 `--status` 指定文件，前端轮询 `message`；stderr 写入 OCR 日志，避免污染 stdout。现有后端的本地模型校验、分批翻译、缓存审计、人工编辑保留和单句重试逻辑继续复用。

## 必须先完成的跨平台适配

1. **路径模块**：缓存从 macOS 的 `~/Library/Application Support/KomaReader/pages` 改为按平台选择；Windows 使用 `%LOCALAPPDATA%\\KomaReader\\pages`，模型和日志仍放项目或用户数据目录。Ollama 可执行文件、虚拟环境解释器也按平台解析：Windows 为 `.venv\\Scripts\\python.exe`，macOS 为 `.venv/bin/python`，Windows Ollama 为 `ollama.exe`。
2. **UTF-8**：worker 所有 JSON 和日志读写显式使用 UTF-8，消除 Windows 依赖 `-X utf8` 才能工作的隐患。
3. **编辑保存**：把缓存读写、`saveEdit`、`retryBlock` 等业务操作收敛到共享 Python 模块；Qt 和 SwiftUI 只负责展示与发送操作，保证手动译文不会被批量翻译覆盖。
4. **Ollama 服务**：两端统一使用 `127.0.0.1:11439`、关闭云端模型、只接受已下载模型；启动、探活和终止由平台适配层处理。

## 四阶段计划与验收标准

### 阶段 1：环境和样图链路

目标：在 Windows RTX 5070（约 12 GB 显存、32 GB 内存）上准备 Python 3.12 虚拟环境、依赖、Ollama 和模型下载流程。

验收：安装器或脚本能完成依赖检查；OCR 模型与本地 Ollama 模型下载状态可诊断；对 `samples/` 至少一张图执行 `ocr` 和 `both`，生成有效缓存、译文和状态日志。当前已在 RTX 5070 上完成样图 OCR → 翻译，4B 和 8B 两个模型均可用；PyTorch 当前为 CPU，OCR 加速仍待单独评估。

网络诊断结论：hosts 中 Steam++ 区块把 Hugging Face 和 GitHub 映射到 `127.0.0.1`，本机 80/443 无监听；经 Clash Verge `7897` 代理并让单次请求使用正确公网解析后，TLS 校验正常且能返回 Hugging Face 元信息/GitHub 下载。长期方案不写死固定 IP；安装器应提供网络诊断，并支持进程级代理/解析选择。对加速规则做持久修复另列为运维事项。

### 阶段 2：共享后端跨平台化

目标：完成路径、UTF-8、进程启动、Ollama 探活和缓存编辑模块，保持 worker 协议稳定。

验收：Windows 与 macOS 各自新建虚拟环境即可运行同一组后端测试；缓存、日志和模型目录落在正确平台目录；取消任务不会损坏已完成缓存；手动译文、单句重译、模型切换和失败重试行为一致。

### 阶段 3：Windows PySide6 阅读器

目标：实现文件夹选择、自然排序缩略列表、图片缩放滚动、原图/文字框/中文三种显示、对白编辑器、进度与停止按钮，并通过 `QProcess` 调用阶段 2 的 worker。

验收：在 Windows 上打开 `samples/`，完成单页 OCR→翻译；能定位文字框、编辑并保存、单句重译、批量翻译后停止；重启程序可读取缓存和上次文件夹；高 DPI 显示、中文路径、WebP/PNG/JPG 均可用；打包后的程序不依赖开发机绝对路径。

### 阶段 4：macOS 复用与双端发布

目标：让 SwiftUI 前端切换到共享后端模块，同时保留现有 macOS 体验；补齐 PySide6 macOS 构建作为统一 UI 的后续选项，分别生成 Windows 安装包和 macOS `.app`。

验收：两端均能从干净机器安装、启动 Ollama、选择模型并处理样图；安装包内路径可迁移，卸载不删除用户缓存（除非用户明确选择）；发布前完成后端回归测试、样图链路测试、取消/断网/模型缺失错误提示和中文路径测试。

## 当前状态记录

- Python 3.12 依赖环境已创建，现有 10 项后端测试通过。
- RTX 5070 约 12 GB 显存、32 GB RAM，可作为 Windows 首版目标硬件；Ollama 已确认 4B 与 8B 均使用 100% GPU，PyTorch 尚未配置 CUDA，OCR 速度待单独验证。
- OCR 模型下载失败原因已定位为 hosts/代理解析冲突；通过正确公网解析和现有代理下载成功，模型已安装。
- Ollama 0.34.0、项目专用 11439 服务、`qwen3:4b` 和 `qwen3:8b` 均已完成安装与推理验证。官方没有 `qwen3:9b` 标签，8B 作为近邻对比规格。
